{% extends 'admin/_layout.html.twig' %}

{% block admin_title %}Modifier {{ link.label }}{% endblock %}

{% block admin_breadcrumb %}
    <a href="{{ path('admin_dashboard') }}" class="hover:underline">Administration</a>
    <span aria-hidden="true">›</span>
    <a href="{{ path('admin_external_links_index') }}" class="hover:underline">Liens externes</a>
    <span aria-hidden="true">›</span>
    <span aria-current="page">{{ link.label }}</span>
{% endblock %}

{% block admin_body %}
    <h1 class="text-2xl font-bold text-slate-900">Modifier « {{ link.label }} »</h1>
    <div class="mt-6 max-w-xl rounded-lg border border-slate-200 bg-white p-6">
        {{ include('admin/external_links/_form.html.twig', {submit_label: 'Enregistrer'}) }}
    </div>
{% endblock %}
